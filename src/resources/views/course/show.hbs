<div class="mt-4">
    <div class="row">
        <div class="col-3">
            <p>Bạn đã đăng kí khóa học này</p>
            <button>Học ngay</button>
        </div>
        <div class="col-9">
            <h2>{{course.name}}</h2>
            <img src="{{course.image}}" alt="">
            <iframe src="https://www.youtube.com/embed?v={{course.videoId}}" width="560" height="315" />
            <p>{{course.descripton}}</p>
        </div>
    </div>
</div>